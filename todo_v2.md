# Ledger Hub 后续需求清单 V2

基于当前项目源码、设计文档、HTTP API、Worker、Gateway、持久化模型和现有测试梳理。

当前 Phase 1–3 的业务能力已经比较完整。下一阶段最值得投入的方向，不是继续增加记账命令，而是补齐多租户安全、分布式一致性和生产可运行性。

状态约定：`- [ ]` 未完成 · 优先级：P0 安全与一致性 / P1 生产运行与接入 / P2 规模化。

---

## P0 · 多租户安全与数据隔离

- [x] **所有按 ID 查询和变更强制校验租户**
  - 当前账户详情、冻结单详情和 Journal 详情等按 ID 查询没有同时校验当前租户。
  - 如果调用方获得其他租户的资源 ID，可能越权读取对应数据。
  - 仓储接口应将 `tenant_id` 纳入查询条件，不能只在 HTTP 层过滤。
  - 账户、冻结、Journal、对账任务、差异单、汇率等资源统一执行租户校验。
  - 增加跨租户查询、状态变更和枚举攻击的回归测试。

- [x] **客户端绑定可访问租户范围**
  - Gateway 的 `client_id` 应配置允许访问的租户列表。
  - 禁止客户端通过请求体或 `X-Tenant-Id` 随意切换到未授权租户。
  - 默认租户只能作为兼容策略，生产环境应要求显式、可信的租户上下文。
  - 审计日志记录最终解析出的 `tenant_id`。

## P0 · Gateway 签名协议 V2

- [x] **扩大签名覆盖范围**
  - 当前签名内容只有 `client_id + timestamp + body`。
  - HTTP method、canonical path、query、`X-Tenant-Id` 等关键请求信息没有参与签名。
  - V2 签名应覆盖：签名版本、client ID、method、canonical path/query、tenant ID、timestamp、nonce 和 body hash。

- [x] **增加 nonce 防重放**
  - timestamp 时间窗只能限制重放时间，不能阻止窗口内重复请求。
  - nonce 在允许时间窗内必须全局唯一，并设置自动过期。
  - 重复 nonce 返回明确的鉴权错误码并写入安全审计。

- [x] **密钥版本、轮换与兼容升级**
  - 请求携带 key version，服务端支持新旧密钥短期共存。
  - SDK 自动生成 V2 签名。
  - 提供 V1/V2 灰度迁移期，迁移完成后关闭 V1。
  - Console token 禁止通过 query string 传递，避免出现在浏览器历史和访问日志中。

## P0 · 跨分片转账可靠性

- [x] **将跨分片 Transfer 改造成持久化 Saga**
  - 当前实现是两次本地事务，第二腿失败时尝试冲正第一腿。
  - 第一腿完成后如果进程崩溃，第二腿可能永远不执行。
  - 第二腿和补偿冲正均可能失败，产生长期单边账。
  - 新增 `ledger_transfer_saga`，记录 `pending / out_done / in_done / compensating / completed / failed` 等状态。

- [x] **跨分片每一腿独立幂等并可续跑**
  - OUT、IN、ROLLBACK 使用稳定且可推导的幂等键。
  - Worker 扫描未完成 Saga，自动续跑或补偿。
  - 最终业务幂等记录保存失败不能被忽略，应进入可恢复状态。
  - 对长期 pending、重复失败和补偿失败产生告警。

- [x] **跨分片一致性运营能力**
  - 提供 Saga 查询、人工重试和人工补偿接口。
  - 增加 pending settlement 系统科目一致性扫描。
  - 对账报告单独输出跨分片在途和异常项。

## P0 · 输入校验与数据库约束

- [x] **严格校验全部金额字段**
  - `from.amount`、`to.amount`、`fee.amount`、`tolerance` 解析失败时不能静默变为 0。
  - 明确每个命令的必填字段、可省略字段、零值和负值规则。
  - 非整数、溢出、空字符串和超长输入统一返回明确的参数错误。
  - 增加 HTTP DTO 边界测试和模糊测试。

- [x] **严格校验查询时间参数**
  - `from`、`to` 非法时返回 400，不能忽略过滤条件后返回更大范围的数据。
  - 校验 `from < to` 和最大查询时间跨度。

- [x] **修正冻结单多租户唯一键**
  - 当前冻结单 `biz_no` 是全表唯一，不同租户使用相同业务号可能互相冲突。
  - 唯一键至少调整为 `(tenant_id, biz_no)`；结合业务规范评估是否应为 `(tenant_id, source_system, biz_no)`。
  - 通过版本化数据库迁移修改约束，并处理历史重复数据。

---

## P1 · Worker 集群安全与任务调度

- [ ] **Worker 多副本互斥执行**
  - 当前每个 Worker 实例都会启动全部 ticker，多副本部署时会重复执行释放、过期、汇率和对账任务。
  - 使用数据库租约、分布式锁或 leader election 保证任务归属。
  - 租约需要支持续期、过期接管和实例异常恢复。

- [ ] **对账任务去重**
  - 对账定时器会周期性为前一天创建新任务，缺少任务级唯一约束。
  - 建议唯一维度为：租户、业务日期、来源系统、资产、任务类型和任务版本。
  - 手工重跑明确区分“复用已有结果”和“强制创建新版本”。

- [ ] **统一后台任务执行框架**
  - 记录任务状态、执行实例、开始时间、完成时间、耗时、重试次数和最后错误。
  - 支持指数退避、最大重试次数、死信状态和人工重试。
  - 运营台展示任务最后成功时间及积压情况。

## P1 · 健康检查与可观测性

- [ ] **区分存活和就绪探针**
  - `/livez` 只检查进程是否存活。
  - `/readyz` 检查 MySQL 主库及各分片、必要配置和后台任务初始化状态。
  - Gateway readiness 应检查上游可用性，但避免健康检查造成级联压力。

- [ ] **Prometheus 指标**
  - 命令请求量、成功率、错误码和延迟分布。
  - 幂等命中、余额不足、账户禁用、ACL/限额拒绝、汇率滑点拒绝。
  - Saga pending 数量和最长 pending 时间。
  - Worker 任务耗时、失败次数、最后成功时间和对账差异数。
  - 数据库连接池、慢查询和各分片请求分布。

- [ ] **OpenTelemetry 链路追踪**
  - 贯通 Gateway、API、Connector、Worker 和数据库访问。
  - 日志统一携带 `request_id`、`trace_id`、`tenant_id`、`client_id`、`biz_no` 和 `journal_id`。

## P1 · 正式数据库迁移机制

- [ ] **用版本化 migration 替代生产 AutoMigrate**
  - API 和 Worker 启动时执行 AutoMigrate，不适合生产多副本和复杂索引变更。
  - 增加独立的 `ledger-migrate` 命令或部署 Job。
  - 记录 schema version、执行结果和失败原因。
  - 生产默认关闭 AutoMigrate，开发环境可选择保留。

- [ ] **支持无停机 schema 演进**
  - 按 expand / migrate / contract 阶段执行兼容发布。
  - 为新增索引、字段回填、唯一键调整提供专用迁移和回滚方案。

## P1 · 密钥与配置管理

- [ ] **生产密钥外置**
  - Gateway secret 和 Console token 只从环境变量、Secret 文件或密钥管理服务读取。
  - 生产环境检测到 `dev-*` 密钥或默认 token 时拒绝启动。
  - 日志和错误响应不得输出密钥内容。

- [ ] **可审计的配置热加载**
  - ACL、限额、客户端租户权限等配置统一持久化或接入配置中心。
  - 配置变更带版本、操作人、校验结果和回滚能力。
  - 多实例应原子切换到同一配置版本。

## P1 · OpenAPI 与 SDK 完整化

- [ ] **补全 OpenAPI 契约**
  - 当前 OpenAPI 未完整覆盖资产、账户状态、租户、汇率、对账、运营和差异处理接口。
  - 补全所有路由、请求模型、成功响应、错误响应、分页、金额格式和示例。
  - 将 OpenAPI 作为 HTTP 接口的唯一契约来源，并在 CI 中检查实现与规范偏差。

- [ ] **生成强类型 SDK**
  - Go SDK 去除核心接口中的 `map[string]interface{}`。
  - 提供强类型 Command、Envelope、错误码、分页和查询结果。
  - 内置超时、V2 签名、request ID、有限重试和幂等调用建议。
  - 后续按接入方需求生成 Java 和 TypeScript SDK。

## P1 · 对账异步化与差异工单

- [ ] **对账触发接口异步返回**
  - `POST /reconcile/jobs` 创建任务后立即返回 `202 + job_id`。
  - Worker 异步领取和执行任务，避免大数据量时超过 HTTP timeout。
  - 提供进度、阶段、结果、失败原因和文件下载状态查询。

- [ ] **完善对账差异工单**
  - 支持差异指派、备注、关闭人、关闭时间和完整审计历史。
  - 支持取消、重试、强制重跑和报告版本管理。
  - 对账文件支持访问权限和下载有效期。

## P1 · 真实 MQ Connector

- [ ] **接入生产消息系统**
  - 当前 MQ 样板实际轮询本地 JSON 文件，不具备生产消息语义。
  - 提供 Kafka 或 RabbitMQ 插件式消费者。
  - 使用 inbox 表实现消费幂等和处理状态持久化。

- [ ] **消息失败治理**
  - 支持重试队列、死信队列、消费位点和失败原因查询。
  - 运营台支持安全重放。
  - 业务事件 schema 增加版本号和兼容策略。

---

## P2 · 数据规模与性能

- [ ] **流水表按月分区与冷热归档**
  - 按业务日期规划分区，提供自动创建未来分区的运维任务。
  - 历史流水迁入归档存储，同时保留受控查询能力。

- [ ] **查询改用游标分页**
  - 热点流水查询使用稳定的 `created_at + id` 游标，避免大 offset 扫描。
  - 明确排序和翻页一致性语义。

- [ ] **余额只读缓存**
  - 数据库仍为唯一真相。
  - 写入成功后可靠失效缓存，缓存异常不能影响记账事务。
  - 增加缓存命中率、陈旧数据检测和降级策略。

- [ ] **热点账户与系统科目治理**
  - 监控账户行锁等待时间。
  - 对高并发系统科目评估分段子账户、分片本地科目和定期归集。

- [ ] **分片扩容、迁移与校验工具**
  - 支持 holder 路由预演、数据双写或停写迁移、校验和切换。
  - 提供全分片账户余额、流水和幂等记录一致性检查。

- [ ] **备份恢复与灾备演练**
  - 明确 RPO/RTO。
  - 定期验证全量备份、增量日志和指定时间点恢复。
  - 对跨分片 Saga、对账文件和配置数据一并制定恢复策略。

## P2 · 数据安全与合规

- [ ] **数据导出权限与脱敏**
  - 对账文件、流水导出和审计查询按角色授权。
  - holder ID、IP 和扩展字段按场景脱敏。

- [ ] **数据保留策略**
  - 分别定义流水、幂等、审计、任务日志和对账文件的保留期限。
  - 删除和归档过程应可审计、可验证。

---

## 工程质量与测试

- [ ] **MySQL 集成测试**
  - 当前测试以应用层 fake 为主，缺少真实 MySQL 的事务、锁、唯一键和迁移验证。
  - 使用 Docker Compose 或 Testcontainers 覆盖开户、并发扣款、冻结、兑换、冲正和对账。
  - 增加多租户唯一键和越权访问测试。

- [ ] **多分片故障注入测试**
  - 覆盖第一腿成功后崩溃、第二腿失败、补偿失败、幂等记录失败和 Worker 接管。
  - 验证最终余额、pending settlement 和 Saga 状态一致。

- [ ] **CI 流水线**
  - 执行格式检查、静态分析、单元测试、race test、集成测试和构建。
  - 校验 OpenAPI、数据库 migration 和 Docker 镜像。
  - 当前 Gateway 测试依赖本地监听端口，可补充不需要真实监听器的 handler 级测试。

---

## 推荐实施顺序

1. 多租户数据隔离和客户端租户授权。
2. Gateway 签名 V2、nonce 和密钥轮换。
3. 金额解析、时间参数、冻结唯一键等确定性缺陷。
4. 跨分片 Transfer Saga、异常续跑和一致性扫描。
5. Worker 任务租约、任务去重和异步对账。
6. readiness、Prometheus、tracing 和结构化日志。
7. 正式数据库 migration、密钥外置和配置版本化。
8. 完整 OpenAPI、强类型 SDK 和真实 MQ。
9. 分区、游标分页、缓存、热点账户和扩片工具。

## 明确保持非目标

- 商品库存。
- 订单状态机。
- 支付渠道路由。
- 券码生命周期。
- 会员等级。
- 行情价格正确性。

这些能力继续由对应业务系统负责；Ledger Hub 保持为资产记账、凭证、流水和对账内核。
