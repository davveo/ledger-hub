# 通用账本中台（Ledger Hub）技术方案

> 状态：草案 v0.2  
> 定位：面向任意交易相关系统的「虚拟资产账本中台」——提供统一开户、入账、出账、冻结、转账、**多币种兑换**、流水与**对账**能力；**不替代**订单、支付、库存、营销等业务系统。  
> 目标：积分 / 余额 / 多币种法币 / 代币 / 内部额度等可计量资产，均可配置化接入；交易系统通过标准记账命令对接。

---

## 1. 背景与目标

### 1.1 问题

业务侧常见做法是 `user.points++`、`balance--` 各写一套：

- 资产类型增加即改表改接口
- 无幂等、易超扣、难对账
- 订单/支付/活动各自改余额，责任不清
- 无法复用到新的交易系统

### 1.2 建设目标

| 目标 | 说明 |
|------|------|
| 通用账本 | 用 `asset_code` 承载积分、余额、金币等，内核同一套 |
| 标准对接 | 任意交易系统仅对接 Credit/Debit/Freeze/Capture/Release/Transfer |
| 强可追溯 | 每笔变动有流水、业务单号、变更后余额 |
| 防重防超 | 幂等键 + 账户乐观锁/行锁 |
| 可对账 | 按日/按业务系统/按币种输出对账视图，支持差异工单 |
| 多币种 | 一币种一资产一账户；跨币种走兑换凭证，禁止混币账户 |
| 快速接入 | 新业务注册资产 + 按接入规范调用，无需改账本核心 |

### 1.3 非目标（明确不做）

- 商品库存、仓配、批次管理（库存中心）
- 订单/履约状态机（交易/订单系统）
- 支付渠道路由、收单清分合规主体（支付中台）
- 优惠券码实例全生命周期（券中心；可与账本联动）
- 会员等级等纯状态权益（权益中心；除非是可计量额度）

### 1.4 一句话定位

> **Ledger Hub = 交易世界的「资产记账内核」；业务系统负责交易，账本负责把钱/分/币记清楚。**

---

## 2. 核心概念

```text
Tenant / App
  └── Asset（资产定义：POINT、BALANCE_CNY、COIN…）
        └── Account（用户或商户账户：holder + asset）
              ├── available / frozen
              └── LedgerEntry（流水）
                    └── 可选归属 Journal（复式凭证）

FreezeOrder（冻结单：预占 → 确认/释放）
```

| 概念 | 定义 |
|------|------|
| **Asset** | 资产类型定义（精度、是否可冻、是否可透支、过期策略） |
| **Account** | 某持有者在某资产下的账户 |
| **Holder** | `user` / `merchant` / `system_subject`（系统科目） |
| **LedgerEntry** | 单笔账户变动流水（真相来源之一） |
| **Journal** | 一笔业务对应的复式凭证（可含多条分录） |
| **biz_no** | 业务幂等键，建议 `"{source_system}:{biz_id}"` |
| **Freeze** | 预占：available → frozen，待确认或释放 |

余额恒等式：

```text
total = available + frozen
```

---

## 3. 总体架构

```text
┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ 订单/交易系统 │  │ 支付/充值    │  │ 活动/营销    │  │ 游戏/其他    │
└──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘
       │                 │                 │                 │
       │         Adapter：业务事件 → 标准记账命令              │
       ▼                 ▼                 ▼                 ▼
┌────────────────────────────────────────────────────────────────────┐
│                         API Gateway                                 │
│                   鉴权 / 限流 / 审计 / 签名                          │
└─────────────────────────────┬──────────────────────────────────────┘
                              ▼
┌────────────────────────────────────────────────────────────────────┐
│                        Ledger Hub                                   │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌──────────────────┐ │
│  │ Asset Mgmt │ │ Account    │ │ Bookkeeping│ │ Freeze Engine    │ │
│  │ 资产注册   │ │ 开户/查询  │ │ 记账引擎   │ │ 冻结/确认/释放   │ │
│  └────────────┘ └────────────┘ └──────┬─────┘ └──────────────────┘ │
│  ┌────────────┐ ┌────────────┐       │        ┌──────────────────┐ │
│  │ Idempotency│ │ Reconcile  │       │        │ Limit / Risk     │ │
│  │ 幂等      │ │ 对账       │       ▼        │ 限额风控（可插）  │ │
│  └────────────┘ └────────────┘  Ledger Store └──────────────────┘ │
└────────────────────────────────────────────────────────────────────┘
```

设计原则：

1. **内核极简**：只认记账命令，不认「下单」「签到」等业务语义（语义放 `biz_type`）。  
2. **扩展配置化**：新资产 = 注册配置；新业务 = 新 `biz_type` + 调用方适配。  
3. **写入强一致，读取可缓存**：余额缓存可失效重算，流水不可丢。

---

## 4. 标准记账命令（通用内核）

所有对接系统只允许使用以下命令：

| 命令 | 含义 | 典型场景 |
|------|------|----------|
| `Credit` | 入账（available↑） | 充值、发奖、退款加回 |
| `Debit` | 出账（available↓） | 直接消费、扣罚 |
| `Freeze` | 预占（available↓ frozen↑） | 下单锁积分/锁余额 |
| `Capture` | 确认预占（frozen↓，落定消耗） | 支付成功、履约成功 |
| `Release` | 释放预占（frozen↓ available↑） | 取消订单、超时关单 |
| `Transfer` | 同币种转账（A↓ B↑，**同一 asset_code**） | 转赠、分账到商户户 |
| `Exchange` | 跨币种兑换（Debit 币种A + Credit 币种B，一笔 Journal） | CNY↔USD、积分兑换余额等 |

> 说明：简单业务可只用 Credit/Debit；涉及「先锁后扣」必须走 Freeze 三态，避免先扣后加回的并发问题。  
> **Transfer 禁止跨币种**；跨币种必须用 `Exchange`，以便保留汇率、双边金额并对账。

### 4.1 统一命令模型

```json
{
  "command": "Freeze",
  "request_id": "可选，链路追踪",
  "tenant_id": "t_default",
  "source_system": "order",
  "biz_type": "order_freeze_point",
  "biz_no": "order:O20260814001",
  "holder": { "type": "user", "id": "u_123" },
  "asset_code": "POINT",
  "amount": "50",
  "currency_unit": "minor",
  "ext": { "order_id": "O20260814001" }
}
```

成功响应（幂等可重复返回同一结果）：

```json
{
  "code": 0,
  "data": {
    "accepted": true,
    "idempotent_replay": false,
    "freeze_id": "fz_xxx",
    "entry_ids": ["le_1"],
    "account": {
      "account_id": "acc_xxx",
      "available": "150",
      "frozen": "50"
    }
  }
}
```

### 4.2 业务事件 → 命令映射（适配层）

| 交易侧事件 | 账本命令 |
|------------|----------|
| 创建待支付订单（积分抵扣） | `Freeze` |
| 支付成功 | `Capture` |
| 取消/超时未支付 | `Release` |
| 售后退积分 | `Credit`（`biz_no=refund:{id}`，`ext.ref_biz_no=原单`） |
| 活动发奖 | `Credit` |
| 钱包扣款（无预占） | `Debit` |
| 用户转赠（同币种） | `Transfer` |
| 充值到账 | `Credit`（常由支付成功消息驱动） |
| 汇兑 / 积分抵扣现金 | `Exchange` |

适配器可放在：

- 业务服务内（SDK 封装），或  
- 独立 Connector（订阅订单/支付 MQ 后记账）

**Ledger Hub 本身不订阅各业务私有事件格式**；由对接方或 Connector 翻译成标准命令。

---

## 5. API 设计

统一前缀：`/api/v1/ledger`  
鉴权：应用 `client_id` + 签名 / mTLS（仅服务端调用，不对公网浏览器暴露 Secret）。

### 5.1 资产与账户

```http
POST /api/v1/ledger/assets                 # 注册/更新资产定义（管理端）
GET  /api/v1/ledger/assets/{asset_code}

POST /api/v1/ledger/accounts/open          # 可懒开户；也可显式开户
GET  /api/v1/ledger/accounts?holder_type=user&holder_id=u_1&asset_code=POINT
GET  /api/v1/ledger/accounts/{account_id}
```

### 5.2 记账命令

```http
POST /api/v1/ledger/commands/credit
POST /api/v1/ledger/commands/debit
POST /api/v1/ledger/commands/freeze
POST /api/v1/ledger/commands/capture      # body: freeze_id 或 biz_no
POST /api/v1/ledger/commands/release
POST /api/v1/ledger/commands/transfer
POST /api/v1/ledger/commands/exchange     # 跨币种兑换
```

### 5.2.1 兑换与汇率（管理/查询）

```http
POST /api/v1/ledger/fx/rates              # 登记/更新汇率（或对接行情源后写入）
GET  /api/v1/ledger/fx/rates?base=CNY&quote=USD&at=2026-08-14T10:00:00Z

POST /api/v1/ledger/commands/exchange
```

兑换请求示例：

```json
{
  "command": "Exchange",
  "source_system": "wallet",
  "biz_type": "fx_convert",
  "biz_no": "wallet:fx:F20260814001",
  "holder": { "type": "user", "id": "u_123" },
  "from": { "asset_code": "BALANCE_CNY", "amount": "10000" },
  "to":   { "asset_code": "BALANCE_USD", "amount": "1400" },
  "fx": {
    "rate_id": "fxr_001",
    "base_asset": "BALANCE_CNY",
    "quote_asset": "BALANCE_USD",
    "rate": "0.14000000",
    "rate_source": "manual|feed",
    "quoted_at": "2026-08-14T10:00:00Z"
  },
  "fee": { "asset_code": "BALANCE_CNY", "amount": "10" },
  "ext": { "scene": "user_convert" }
}
```

约定：

- `from.amount` / `to.amount` / `fee.amount` 均为**最小单位整数**（分）  
- `rate` 表示：`to_display ≈ from_display * rate`（需与资产精度一同校验）  
- 账本校验：`|to - from*rate|` 在允许滑点/`tolerance` 内，否则拒绝  
- 一笔 `Exchange` 生成**同一 `journal_id`** 下多条 `ledger_entry`（出金、入金、可选手续费）

亦提供聚合入口（便于网关路由）：

```http
POST /api/v1/ledger/commands
{ "command": "Credit", ... }
```

### 5.3 流水与冻结查询

```http
GET /api/v1/ledger/entries?biz_no=order:O001
GET /api/v1/ledger/entries?holder_id=u_1&asset_code=POINT&from=&to=

GET /api/v1/ledger/freezes/{freeze_id}
GET /api/v1/ledger/freezes?biz_no=order:O001
```

### 5.4 对账

```http
POST /api/v1/ledger/reconcile/jobs        # 触发某日对账任务
GET  /api/v1/ledger/reconcile/jobs/{id}
GET  /api/v1/ledger/reconcile/reports/{date}?source_system=order
```

---

## 6. 数据模型

### 6.1 `ledger_asset`

| 字段 | 说明 |
|------|------|
| tenant_id | |
| asset_code | 唯一，如 `POINT`、`BALANCE_CNY` |
| name | 展示名 |
| asset_class | `fiat` / `points` / `token` / `other` |
| currency_code | ISO 4217 或自定义，如 `CNY`/`USD`/`POINT`（与 asset_code 可 1:1） |
| precision | 小数位；落库用最小单位 |
| holder_types | 允许的持有者类型 |
| freeze_supported | |
| overdraft_allowed | 默认 false |
| status | active/disabled |
| ext | 过期策略、展示符号等 |

> **一币种一资产一账户**：禁止在同一 `account` 混存 CNY 与 USD。多币种 = 注册多个 asset + 多个账户。

### 6.2 `ledger_account`

| 字段 | 说明 |
|------|------|
| account_id | |
| tenant_id | |
| holder_type / holder_id | |
| asset_code | |
| available / frozen | **BIGINT 最小单位** |
| version | 乐观锁 |
| status | |
| unique(tenant, holder_type, holder_id, asset_code) | |

**金额存储建议：** 一律最小单位整数（分、积分个），API 层可转小数展示，避免浮点误差。

### 6.3 `ledger_entry`

| 字段 | 说明 |
|------|------|
| entry_id | |
| account_id / tenant_id / asset_code | |
| holder_type / holder_id | |
| direction | IN / OUT |
| amount | 最小单位（**单笔流水只含一种 asset_code**） |
| available_after / frozen_after | 变更后快照 |
| command | Credit/Debit/Exchange/... |
| source_system / biz_type / biz_no | |
| journal_id / freeze_id | 可空；兑换必填 journal_id |
| related_biz_no | 退款/冲正关联原单 |
| created_at | |
| index(biz_no) / index(holder, asset, time) / index(journal_id) | |

### 6.4 `ledger_journal`（复式，余额类与兑换推荐）

| 字段 | 说明 |
|------|------|
| journal_id | |
| biz_no | 与命令幂等键一致 |
| journal_type | `transfer` / `exchange` / `posting` / `reverse` |
| status | posted |
| entries_count | |
| fx_rate_id | 兑换时关联汇率快照 |
| ext | 冗余汇率、滑点等便于查询 |

系统科目示例：`system:point_issuance`、`system:point_sink`、`system:pending_settlement`、`system:fx_clearing`。

### 6.5 `ledger_freeze`

| 字段 | 说明 |
|------|------|
| freeze_id | |
| biz_no | 唯一 |
| account_id / amount | |
| asset_code | 冗余便于按币种查询 |
| status | `frozen` / `captured` / `released` |
| expire_at | 可选超时自动释放（任务扫） |

### 6.6 `ledger_idempotency`

| 字段 | 说明 |
|------|------|
| tenant_id + source_system + biz_no + command | 或统一 request hash |
| response_json | 首次成功响应缓存 |
| created_at | |

> 同一 `biz_no` 重复 Credit 不得再次加钱；Capture/Release 对同一 freeze 也必须幂等。

### 6.7 `ledger_fx_rate`（汇率快照）

| 字段 | 说明 |
|------|------|
| rate_id | 主键 |
| tenant_id | |
| base_asset / quote_asset | 如 BALANCE_CNY / BALANCE_USD |
| rate | 定点小数或缩放整数（需约定 scale，如 1e8） |
| rate_source | `manual` / `feed` / `partner` |
| valid_from / valid_to | 有效期 |
| quoted_at | 行情时间 |
| created_by / created_at | 审计 |

### 6.8 `ledger_exchange_leg`（可选，兑换腿明细）

若不想只靠 journal+entry 推断，可冗余一笔兑换头：

| 字段 | 说明 |
|------|------|
| exchange_id / journal_id / biz_no | |
| holder | |
| from_asset / from_amount | |
| to_asset / to_amount | |
| fee_asset / fee_amount | 可空 |
| rate_id / rate | 成交使用的汇率快照 |
| status | posted / reversed |
---

## 7. 关键时序

### 7.1 下单预占 → 支付成功

```text
Order                     Ledger Hub
  |-- Freeze(biz_no=order:O1, 50 POINT) -->|
  |<-- freeze_id=fz1, available-=50 -------|
  |   ... 用户支付 ...                     |
  |-- Capture(freeze_id=fz1) ------------->|
  |<-- frozen-=50, 消费落定 ---------------|
```

### 7.2 取消订单

```text
Order                     Ledger Hub
  |-- Release(freeze_id=fz1) ------------->|
  |<-- frozen-=50, available+=50 ----------|
```

### 7.3 支付成功入账（充值）

```text
Payment  --MQ-->  Connector  --Credit(biz_no=pay:P1)-->  Ledger Hub
```

### 7.4 并发扣减（乐观锁）

```text
BEGIN
  SELECT available, version FROM account WHERE id=? FOR UPDATE  -- 或 version CAS
  IF available < amount THEN FAIL
  UPDATE account SET available=available-amount, version=version+1
  INSERT ledger_entry ...
  INSERT idempotency ...
COMMIT
```

---

## 8. 交易系统接入规范（Adapter Contract）

本章是「适用于任何交易相关系统对接」的契约核心。

### 8.1 接入前清单

1. 申请 `source_system` 标识（如 `order`、`pay`、`campaign`）  
2. 注册所需 `asset_code`（精度、是否冻结、限额）  
3. 约定 `biz_no` 生成规则（必须稳定、可重试）  
4. 明确哪些业务走 Freeze 三态，哪些直接 Debit/Credit  
5. 配置对账周期与联系人  

### 8.2 `biz_no` 规范

```text
{source_system}:{biz_type_short}:{natural_id}
例：
  order:freeze:O20260814001
  order:capture:O20260814001
  pay:credit:P988331
  campaign:signin:u123:20260814
```

要求：

- 同一业务动作重试，`biz_no` 不变  
- 逆向业务使用新号，并用 `related_biz_no` 指向原单  
- 禁止随机 UUID 当唯一业务键（除非业务侧持久化该 UUID）  

### 8.3 必须遵守的语义

| 规则 | 说明 |
|------|------|
| 账本无业务状态机 | 不在账本存「订单已发货」 |
| 余额不足 | Debit/Freeze 返回明确错误码，由业务决定取消或提示 |
| 禁止补偿式瞎加 | 取消必须 Release 对应 freeze，或按原入账冲正 |
| 服务端调用 | 记账 API 只能业务后端调，客户端不能直连加款 |
| 最小单位 | 传参与落库统一最小单位 |

### 8.4 错误码（示例）

| code | 含义 |
|------|------|
| 0 | 成功 |
| 40901 | 幂等冲突但命令参数不一致 |
| 42201 | 余额不足 |
| 42202 | 冻结单状态不允许 Capture/Release |
| 40401 | 账户/冻结单不存在 |
| 40001 | 参数错误 / 资产未注册 |
| 42901 | 触发限额 |

### 8.5 SDK 建议

提供 `ledger-go` / `ledger-java` / `ledger-node`：

```go
client.Freeze(ctx, FreezeReq{BizNo: "order:freeze:O1", UserID: "u1", Asset: "POINT", Amount: 50})
client.Capture(ctx, CaptureReq{FreezeID: "fz1", BizNo: "order:capture:O1"})
```

内置：超时重试（仅对幂等安全请求）、错误码映射、本地日志 `request_id`。

### 8.6 接入验收用例

1. 同一 `biz_no` 调用两次 Credit，余额只加一次  
2. Freeze 后余额不足的并发 Debit 失败且不损坏冻结  
3. Capture 后重复 Capture 幂等成功、不重复扣  
4. Release 后不可再 Capture  
5. 日终：业务侧成功单量与账本 entries 按 `biz_no` 对齐  

---

## 9. 安全、限额与审计

| 项 | 方案 |
|----|------|
| 鉴权 | 应用级凭证 + 双向 TLS（内网可放宽为 mTLS/网格身份） |
| 防篡改 | 关键字段签名或网关验签 |
| 限额 | 单笔/日累计/用户维度（按 asset 配置） |
| 审计 | 所有命令落审计日志（谁在何时以何 biz_no 记账） |
| 权限 | 仅授权系统可 Credit 某资产（如只有 campaign 能发 POINT） |
| 回放 | 幂等表保存响应，禁止「换金额重放成功」 |

资产发放权限矩阵示例：

```text
campaign  → Credit POINT
order     → Freeze/Capture/Release POINT
pay       → Credit BALANCE_CNY
*         → 禁止任意系统给自己加余额
```

---

## 10. 多币种与兑换

### 10.1 模型原则

```text
币种/积分/平台币  →  注册为不同 asset_code
用户持有多币种    →  多个 Account（每种资产一个）
同币种转移        →  Transfer
跨币种转换        →  Exchange（一笔 Journal，多条单币种流水）
```

禁止事项：

- 禁止一个账户字段里同时存多币种余额  
- 禁止用两次独立 Credit/Debit 冒充兑换却不共享 `journal_id` / `biz_no`（会导致对账无法证明「一笔兑换」）  
- 禁止 Transfer 跨 `asset_code`

### 10.2 账户视图示例

```text
user u_123
  ├── BALANCE_CNY  available=50000  frozen=0     # ¥500.00
  ├── BALANCE_USD  available=1400   frozen=0     # $14.00
  └── POINT        available=200    frozen=50
```

### 10.3 兑换记账拆解（CNY → USD）

用户兑换：扣 100.00 CNY + 手续费 0.10 CNY，按汇率得到 14.00 USD。

| 分录 | 账户 | 方向 | amount（最小单位） | asset |
|------|------|------|-------------------|-------|
| 1 | user CNY | OUT | 10000 | BALANCE_CNY |
| 2 | user CNY | OUT | 10 | BALANCE_CNY（fee） |
| 3 | user USD | IN | 1400 | BALANCE_USD |
| 4 | system:fx_fee_income | IN | 10 | BALANCE_CNY |
| 5 | system:fx_clearing | 轧差/对销 | 按实现可选 | — |

全部挂在同一 `journal_id` + `biz_no=wallet:fx:F001`，并写入 `rate_id/rate/quoted_at`。

校验公式（示意）：

```text
expected_to = floor(from_amount_display * rate * to_scale)
|expected_to - to_amount| <= tolerance   # tolerance 按资产配置，如 1 个最小单位
```

### 10.4 汇率来源与责任边界

| 模式 | 说明 | 适用 |
|------|------|------|
| 业务算好双边金额 | 调用方传 `from/to/fee`，账本做守恒与滑点校验 | 最常见，账本不承担行情 |
| 账本按 rate_id 计价 | 调用方只传 from + rate_id，账本计算 to | 需中台维护汇率表 |
| 外部牌价 | 定时任务写入 `ledger_fx_rate` | 有行情源时 |

**汇率本身的对错由报价源/业务负责；账本负责：快照留存、记账守恒、可追述。**

### 10.5 与积分、法币混合场景

| 场景 | 命令 |
|------|------|
| 下单冻结 50 积分 | Freeze(POINT) |
| 积分商城「100 积分换 1 元余额」 | Exchange(POINT→BALANCE_CNY) |
| 仅人民币余额支付 | Freeze/Capture(BALANCE_CNY) |
| 多币种钱包展示 | 按 asset_code 批量查账户，前端分别展示 |

---

## 11. 对账与运营

### 11.1 对账目标

回答三类问题：

1. **业务是否都记上了？**（业务单 ↔ 账本 `biz_no`）  
2. **账有没有记错金额/币种？**  
3. **账户余额能不能被流水解释？**（勾稽）  

### 11.2 日终对账流程

1. 账本按 `source_system + asset_code + date` 汇总出入发生额、成功笔数  
2. 业务系统导出「应记账成功」清单：`biz_no, command, asset_code, amount, [fx对端金额]`  
3. 以 `biz_no` 为主键做匹配：多账 / 少账 / 金额不一致 / 币种不一致  
4. 出差异工单：补账或冲正（**一律走命令**，禁止直接改余额字段）  

```http
POST /api/v1/ledger/reconcile/jobs
GET  /api/v1/ledger/reconcile/jobs/{id}
GET  /api/v1/ledger/reconcile/reports/{date}?source_system=order&asset_code=BALANCE_CNY
```

### 11.3 对账分层

| 层级 | 对什么 | 规则 |
|------|--------|------|
| L1 业务对账 | 订单/支付/活动 ↔ Entry/Journal | `biz_no` 一对一或已知一对多 |
| L2 余额勾稽 | 账户余额 vs 流水轧差 | `available+frozen == 期初 + ΣIN - ΣOUT`（按账户） |
| L3 冻结勾稽 | freeze 表 vs frozen 字段 | 状态为 frozen 的金额之和 = account.frozen |
| L4 兑换对账 | Exchange Journal | 同一 journal 下 from/to/fee 完整；汇率快照可查 |
| L5 跨系统资金（可选） | 支付渠道成功金额 ↔ Credit | 由支付中台与账本对，按币种分开 |

**多币种铁律：不同 `asset_code` 分文件、分报表、分合计，禁止折本币后混加作为唯一对账结果（折算报表可另出，不替代分币种对账）。**

### 11.4 对账示例

#### 示例 A：积分订单（单币种）

业务导出：

| biz_no | command | asset | amount |
|--------|---------|-------|--------|
| order:freeze:O1 | Freeze | POINT | 50 |
| order:capture:O1 | Capture | POINT | 50 |

账本结果：两条成功记录且金额一致 → 通过。  
若业务有 capture、账本无记录 → **少账**，触发补 Capture 或关单调查。

#### 示例 B：人民币充值

| 支付单 | 账本 Credit |
|--------|-------------|
| pay:P9 成功 CNY 100.00 | biz_no=`pay:credit:P9` amount=10000 asset=BALANCE_CNY |

金额或币种不一致 → **差异**；重复 Credit 被幂等挡掉则业务侧重试应对齐「已成功」。

#### 示例 C：跨币种兑换

业务：`wallet:fx:F1`  CNY 100.00 → USD 14.00，fee 0.10 CNY  

账本 Journal `j1` 应包含：

- OUT CNY 10000  
- OUT CNY 10（fee）  
- IN USD 1400  

对账检查：

1. `biz_no` 存在且 journal_type=exchange  
2. 三条（或含系统科目）分录币种/金额与业务一致  
3. `rate_id` 可追溯到当日牌价  
4. 不要求 CNY 发生额与 USD 发生额相加相等（无意义），只要求**兑换等式与费率**成立  

#### 示例 D：余额勾稽（单账户）

```text
账户 BALANCE_CNY / u_123
期初 available=10000 frozen=0
当日流水：+50000 -10000 -10
期末应有 available=49990
与 account.available 比对，不等则内部差错
```

### 11.5 冲正原则

- 用新 `biz_no` 做反向 Credit/Debit/Exchange，或专用 `Reverse`（内部仍落流水）  
- 关联 `related_biz_no`  
- 保留原流水，不做物理删除  
- 兑换冲正必须整笔 Journal 回滚（双边+手续费），禁止只改一侧  

### 11.6 积分过期（可选模块）

- 策略配在 asset（如「年底清零」「滚动 12 个月」）  
- 到期批任务计算应扣量 → `Debit` + `biz_type=expire`  
- 同样产生流水，可追溯，并进入日终 POINT 对账文件  

### 11.7 对账输出物（建议）

| 文件/报表 | 内容 |
|-----------|------|
| `recon_{sys}_{asset}_{date}.csv` | 账本侧成功明细 |
| `diff_{sys}_{asset}_{date}.csv` | 多账/少账/不一致 |
| `balance_tie_out_{date}.csv` | 账户勾稽失败列表 |
| `fx_journal_{date}.csv` | 兑换凭证及汇率快照 |

---

## 12. 高可用与性能

| 项 | 建议 |
|----|------|
| 部署 | 无状态服务多副本；DB 强一致主库写入 |
| 账户热点 | 单用户单资产串行化（行锁）；大V可分段子账户（后期） |
| 分库 | 按 `holder_id` 哈希；`biz_no` 幂等表同路由 |
| 缓存 | 余额只读缓存，写入后删缓存；不以 Redis 为唯一账本（除非有成熟对账补单） |
| 吞吐 | 流水表按时间分区；查询走索引，不扫全表 |
| 多币种 | 账户与流水按 asset 隔离，便于分片与对账并行 |

真资金/可兑换余额：**不要**只做 Redis 加减；积分高并发可「预扣缓存 + 异步落账」，但必须有对账与补单，且需单独评审。

---

## 13. 分阶段交付

### Phase 1 — MVP 通用账本

- 资产注册（含 currency_code/precision）、懒开户  
- Credit / Debit  
- ledger_entry + 幂等  
- 账户查询、流水查询（按 asset_code）  
- 应用鉴权与基础 SDK  

### Phase 2 — 交易对接完备

- Freeze / Capture / Release  
- Transfer（同币种）  
- 权限矩阵（谁能发哪种资产）  
- **日终对账报表与差异工单（按 source_system + asset_code）**  
- 订单/支付各完成一个样板接入  

### Phase 3 — 多币种与增强

- **Exchange 兑换 + fx_rate 快照 + 兑换对账报表**  
- 复式 Journal 与系统科目（含 fx_clearing）  
- 过期引擎、自动释放超时冻结  
- 多租户强化、分库分表  
- 风控限额、运营控制台  

---

## 14. 适用性结论

| 问题 | 结论 |
|------|------|
| 能否抽象成通用系统？ | **能**，以 Ledger Hub 形式 |
| 能否对接任何交易相关系统？ | **能**，前提是走 Adapter Contract（事件→标准命令） |
| 能否替代一切交易系统？ | **不能**；只替代「各业务私有加减余额」 |
| 是否支持多币种？ | **支持**；一币种一资产一账户，跨币种走 Exchange |
| 是否支持对账？ | **支持**；业务对账 + 余额勾稽 + 分币种/兑换对账 |

通用边界：

```text
可通用：可计量资产的记账、冻结、转账、兑换、流水、对账
不通用：订单流程、支付渠道、库存、行业玩法、汇率定价策略本身
```

---

## 15. 风险与对策

| 风险 | 对策 |
|------|------|
| 业务把状态机塞进账本 | 评审拒绝；仅保留记账命令 |
| 无幂等导致重复加款 | 强制 biz_no；重复请求返回首次结果 |
| 超扣 | 事务内校验余额 + 行锁/乐观锁 |
| 对接方乱 Credit | 资产发放权限矩阵 |
| 对不上账 | 日终对账 + 禁止手工改余额 |
| 多币种混账户/混合计 | 强制单币种账户；对账按 asset 拆分 |
| 假兑换（两侧无 journal） | Exchange 必须同 journal_id；对账校验兑换完整性 |
| 汇率争议 | 落 rate 快照；账本不负责行情对错，负责可追溯 |
| 模型膨胀 | 内核稳定；行业逻辑放 Adapter |

---

## 16. 附录

### 附录 A：命令处理伪代码（Debit）

```text
function Debit(cmd):
  if idempotency.exists(cmd.biz_no, Debit):
     return idempotency.response
  tx:
    acc = lock_account(cmd.holder, cmd.asset)
    if acc.available < cmd.amount: error INSUFFICIENT
    acc.available -= cmd.amount
    entry = write_entry(OUT, ...)
    save_idempotency(cmd.biz_no, response)
  return response
```

### 附录 B：样板资产

| asset_code | currency_code | precision | freeze | 说明 |
|------------|---------------|-----------|--------|------|
| POINT | POINT | 0 | yes | 积分 |
| BALANCE_CNY | CNY | 2 | yes | 人民币余额（分） |
| BALANCE_USD | USD | 2 | yes | 美元余额（分） |
| BALANCE_HKD | HKD | 2 | yes | 港币余额（分） |
| COIN | COIN | 0 | yes | 平台币 |
| GROWTH | GROWTH | 0 | no | 成长值（通常只 Credit） |

### 附录 C：与认证中台关系

- 用户身份由认证中台提供 `user_id`  
- 账本不负责登录；只信任内网已鉴权的服务调用与合法 holder_id  
- 商户账户可与主体服务对齐 `merchant_id`

### 附录 D：Exchange 处理伪代码

```text
function Exchange(cmd):
  if idempotency.exists(cmd.biz_no, Exchange): return cached
  validate_assets_and_tolerance(cmd.from, cmd.to, cmd.fx)
  tx:
    acc_from = lock_account(holder, from_asset)
    acc_to   = lock_account(holder, to_asset)   # 固定加锁顺序防死锁
    ensure_sufficient(acc_from, from_amount + fee)
    journal = open_journal(exchange, cmd.biz_no, cmd.fx.rate_id)
    post OUT from_amount on acc_from
    if fee: post OUT fee; post IN fee on system fee income
    post IN  to_amount on acc_to
    save_idempotency(...)
  return journal_id + balances
```

---

## 17. 文档修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-08-14 | 初稿：通用账本定位、命令集、接入规范、分期交付 |
| v0.2 | 2026-08-14 | 补充多币种/兑换模型、汇率快照、对账分层与示例 |
